面试官好，我叫蔡澈，就职于中建斯维尔公司，我的工作内容主要是多模态数据集构建、大模型微调和agent系统开发。
第一个项目是通过分析施工现场视频或图片来实现工程项目的进度管理、以及现场的安全管理。在这个业务场景中，我主要解决了以下几个问题：
首先基于开源模型groundingDINO、sam与qwen3构建了多模态数据集自动标注pipeline，节省了人工标注成本，
其次在上一步构建的数据集基础上，通过lora微调多模态大模型lisa，实现了开放语义分割，相比传统的视觉模型，可以实现复杂指令的推理分割，比如以往的公司的模型
只能识别安全帽、防光背心等

### 数据集构建：基于 GroundingDINO、SAM、Qwen3 搭建半自动标注 Pipeline，将人工标注转变为人工审核，数据构建效率提升 40%；构建高质量施工场景训练数据，为后续模型微调提供数据支撑。

#### 部署性能
配置：1个DINO、1个SAM、4个Qwen Worker、轮询间隔0.1秒、SAM热缓存。
并发4，P95约6秒；并发8，P95约9秒。Qwen Worker数：4。

#### 模型相关
- groundingDINO
输入：prompt，指明要分割的目标，比如安全帽；图片。
输出：包围框。
推理配置参数：图片尺寸：短边800，长边1333。包围框置信度、token置信度
- 


### 基于 DeepSpeed + LoRA 完成多模态模型 SFT，利用半自动构建的数据集进行训练，设计 Instruction Augmentation（难负样本、具体/抽象多层级prompt）及采样率优化，gIoU 较 Benchmark 提升 11.2%。


## 三大模块完整数据流

输入
├── 原始图像
└── 自然语言 Prompt
        │
        ├────────────────────────────────────────────┐
        │                                            │
        ▼                                            ▼
① 多模态大模型                              ② 视觉 Backbone

原图 → CLIP预处理                         原图 → SAM预处理
       [3,224,224]                                [3,1024,1024]
            │                                           │
            ▼                                           ▼
     CLIP ViT-L/14                              SAM ViT-H Encoder
      [256,1024]                                  [256,64,64]
            │                                           │
            ▼                                           │
     mm_projector                                      │
      1024→5120                                        │
            │                                           │
            ├── 与文本Embedding拼接                     │
            ▼                                           │
       LLaMA-13B                                        │
            │                                           │
            ├──→ lm_head → 文本回答                     │
            │                                           │
            ▼                                           │
   预测SEG的Hidden State                                │
        [Nseg,5120]                                     │
            │                                           │
            ▼                                           │
   text_hidden_fcs                                      │
       5120→256                                         │
            │                                           │
            ▼                                           │
    语言Prompt [Nseg,256]                               │
            │                                           │
            └────────────────┬──────────────────────────┘
                             ▼
                         ③ Decoder

             SAM Prompt Encoder
                       │
                       ▼
              Sparse/Dense Prompt
                       │
                       ├── SAM Image Features
                       ▼
              SAM Mask Decoder
                       │
                       ▼
           Low-res Mask [N,1,256,256]
                       │
                       ▼
                Postprocess
                       │
                       ▼
             最终 Mask [N,H,W]
             
#### DeepSpeed 是什么？

DeepSpeed 是微软开源的分布式深度学习训练框架，主要用于降低大模型训练的显存占用、提升多卡训练效率。它并不是一种微调方法，而是训练加速和显存优化工具；LoRA 负责减少需要训练的参数量，DeepSpeed 负责把模型、梯度和优化器状态更高效地放到多张 GPU 或 CPU 上运行，两者可以配合使用。

训练时，GPU 显存主要被以下几部分占用：

- 模型参数；
- 反向传播产生的梯度；
- Adam 的一阶、二阶动量等优化器状态；
- 前向计算保存的激活值和临时张量。

##### 优化器状态是什么意思？

优化器状态是优化器为了决定“下一步如何更新参数”而长期保存的辅助数据。它不是学习率、权重衰减这类配置参数，也不是本轮刚计算出的梯度，而是随训练步骤不断更新的张量。

以 AdamW 为例，模型中的每个可训练参数通常都有两份同样大小的状态：

- **一阶动量 `m`**：梯度的指数移动平均，反映近期梯度的大致方向；
- **二阶动量 `v`**：梯度平方的指数移动平均，反映梯度的波动幅度。

可以简单理解为：梯度告诉优化器“这一步往哪里走”，`m` 帮助它保持稳定的前进方向，`v` 用于调整每个参数的实际步长。AdamW 根据当前梯度更新 `m` 和 `v`，再用它们计算参数更新量。Momentum SGD 通常只保存一份动量状态，而普通 SGD 不使用动量时可以没有额外的优化器状态。

优化器状态之所以重要，是因为它可能比模型参数本身更占显存。例如，一个参数使用 FP16/BF16 保存需要 2 Byte，而 Adam 的 `m` 和 `v` 通常以 FP32 保存，分别需要 4 Byte，也就是每个可训练参数仅两份动量就需要 8 Byte；混合精度训练还可能额外保存一份 4 Byte 的 FP32 主参数。因此，DeepSpeed ZeRO-1 仅切分优化器状态，就能明显降低多卡训练中的显存冗余。

普通数据并行会在每张 GPU 上保存一份完整的模型参数、梯度和优化器状态，存在大量冗余。DeepSpeed 最核心的能力是 **ZeRO（Zero Redundancy Optimizer）**，它将这些状态切分到不同 GPU 上：

- **ZeRO-1**：切分优化器状态，通信开销较小；
- **ZeRO-2**：进一步切分梯度，通常在显存节省和训练效率之间比较均衡；
- **ZeRO-3**：连模型参数也切分，每张卡只保存其中一部分，显存节省最多，但前向和反向计算时需要频繁聚合参数，通信开销也最大。

DeepSpeed 还支持以下常用优化：

- **CPU/NVMe Offload**：把优化器状态或模型参数卸载到 CPU 内存、NVMe，进一步降低显存占用，但数据搬运会降低训练速度；
- **FP16/BF16 混合精度**：减少显存和计算开销，其中 BF16 的数值范围更大，训练通常更稳定；
- **Gradient Accumulation**：通过多次小批次前向、反向后再更新参数，在有限显存下实现更大的有效 Batch Size；
- **Activation Checkpointing**：不保存全部中间激活，反向传播时重新计算，以计算时间换显存；
- **通信与算子优化**：将梯度规约、参数通信与计算尽量重叠，提高多卡利用率。


> 项目使用 DeepSpeed 作为 LISA 多模态大模型的训练执行框架，通过 ZeRO-2 切分梯度和优化器状态，结合 BF16、梯度累积、梯度裁剪、WarmupDecayLR 和分布式 checkpoint 降低训练显存并管理训练过程；
> 训练完成后使用 zero_to_fp32.py 汇总 ZeRO 权重，再合并 LoRA 导出 Hugging Face 模型。在 FP16 交互推理中，还使用了 DeepSpeed Inference 的自动 kernel injection。
> 当前正式 LoRA 实验默认是单卡 BF16，因此主要受益于 Engine、混合精度、梯度累积和 checkpoint，ZeRO-2 的跨卡切分收益需要多卡运行才能体现。

本项目中，DeepSpeed 的角色可以概括为：负责训练执行与显存优化，不负责 LISA 模型结构本身。主要用了 7 类能力：

DeepSpeed 功能              项目中的代码位置    作用
━━━━━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
分布式启动与 Engine 封装    train_ds.py:347     包装模型、优化器、DataLoader、Scheduler
──────────────────────────  ──────────────────  ──────────────────────────────────────────────
ZeRO Stage 2                train_ds.py:338     切分梯度和优化器状态，降低多卡显存占用
──────────────────────────  ──────────────────  ──────────────────────────────────────────────
BF16/FP16 混合精度          train_ds.py:331     降低显存、提升 Tensor Core 吞吐
──────────────────────────  ──────────────────  ──────────────────────────────────────────────
梯度累积与梯度裁剪          train_ds.py:309     小显存模拟更大的有效 batch
──────────────────────────  ──────────────────  ──────────────────────────────────────────────
优化器和学习率调度          train_ds.py:311     AdamW、WarmupDecayLR
──────────────────────────  ──────────────────  ──────────────────────────────────────────────
分片 checkpoint             train_ds.py:361     保存/恢复模型、优化器和调度器状态
──────────────────────────  ──────────────────  ──────────────────────────────────────────────
推理 Kernel Injection       app.py:137          FP16 推理时自动替换支持的 Transformer kernel

#### 在本项目中为什么使用 DeepSpeed + LoRA？

多模态模型除了语言模型，还包含视觉编码器、投影层和分割模块，即使使用 LoRA，仅训练少量适配器参数，前向和反向传播仍需要加载基础模型，并保存激活值，因此显存压力依然较大。项目中使用 LoRA 降低可训练参数量，再通过 DeepSpeed ZeRO 切分训练状态，并结合 BF16、梯度累积等策略，在有限 GPU 资源上完成多卡 SFT。

需要注意的是：LoRA 冻结的大部分基础模型参数没有梯度和 Adam 状态，因此 LoRA 已经显著降低了这两部分显存。此时直接使用 ZeRO-3 不一定最快，应根据模型大小和硬件选择：模型能够放入单卡或使用 ZeRO-2 已经满足显存要求时，优先选择 ZeRO-2；只有参数本身仍放不下时，才考虑 ZeRO-3 或 Offload。

#### 口述版

DeepSpeed 是一个大模型分布式训练和显存优化框架。它的核心是 ZeRO，通过在多张 GPU 之间切分优化器状态、梯度和模型参数，消除普通数据并行中的显存冗余。ZeRO-1、2、3 分别切分优化器状态、梯度和参数，阶段越高越省显存，但通信成本也越高。在我的项目里，LoRA 用于减少可训练参数，DeepSpeed 用于管理多卡训练、混合精度、梯度累积和训练状态切分。两者结合后，能够在有限显存下完成多模态模型 SFT；实际选型时我会优先满足显存约束，再权衡吞吐量，而不是默认使用最高阶段的 ZeRO-3。




### 推理部署：基于 vLLM + FastAPI 完成模型服务部署，支持并发控制、超时处理及 PNG Base64 Mask 输出，A100 推理耗时 0.416 s/样本。

参数	作用	常见值
--model	模型路径/模型名	/model、Qwen/Qwen3-8B
--served-model-name	API 对外暴露的模型名	qwen3-8b
--host	服务监听地址	0.0.0.0
--port	服务端口	8000
--tensor-parallel-size	张量并行 GPU 数量	1/2/4
--gpu-memory-utilization	vLLM 最多使用多少比例显存	0.8~0.95
--max-model-len	最大上下文长度	8192/16384/32768
--max-num-seqs	同时调度的最大序列数	如 32/64/128
--max-num-batched-tokens	单轮调度最大 token 数	按吞吐调优
--dtype	推理数据类型	auto/float16/bfloat16
--quantization	量化方法	awq/gptq/fp8 等
--enable-prefix-caching	开启 Prefix Cache	适合重复长 Prompt
--api-key	API 访问密钥	如 xxx

| 问题类型       | 常见现象               | 常见解决办法                       |
| ---------- | ------------------ | ---------------------------- |
| 数据质量差      | Loss 降但效果差         | 清洗、去重、修正标注                   |
| 数据不平衡      | 小类别学不好             | 重采样、class weight、Focal Loss  |
| OOM        | CUDA out of memory | 减 batch、LoRA、梯度累积、ZeRO       |
| Loss 不下降   | 模型不收敛              | 检查数据/标签/LR/梯度                |
| Loss 震荡    | 训练不稳定              | 降学习率、warmup、梯度裁剪             |
| 过拟合        | train 好，val 差      | 增加数据、正则化、early stopping      |
| 欠拟合        | train/val 都差       | 增训练量、调 LR、增加可训练参数            |
| 灾难性遗忘      | 垂直能力提升，通用能力下降      | 降 LR、混入通用数据、LoRA             |
| 微调没效果      | 和 base 差不多         | 检查数据、模板、LoRA 参数              |
| 推理效果与训练不一致 | 离线好、上线差            | 对齐 prompt/template/tokenizer |



### 构建基于 YOLO + SORT 的施工人员目标检测与多目标跟踪链路，实现边缘计算设备关键帧自动提取与上传，降低视频传输及平台处理成本；视频摘要 mAP > 90%、Recall > 90%，边缘侧处理速度 ≥ 25 FPS。
### 基于 ResNet3D 构建施工动作识别模型，结合目标跟踪结果统计施工人员动作时长，实现施工进度自动分析；施工进度分析模型平均准确率 ≥ 95%。

> 回答前应将下文中的 `[YOLO 版本]`、`[设备型号]`、`[输入分辨率]`、`[数据规模]`、`[动作类别]` 和指标口径替换为项目真实信息。面试中不要编造没有实际测量的数据。

## 一、YOLO + SORT 目标检测与跟踪面试问答

### 1. 请整体介绍一下这条链路，你具体负责什么？

**参考回答：**

这个项目的目标不是把施工现场的完整视频都上传到平台，而是在边缘设备上先分析视频，只上传能够代表人员活动或关键事件的帧和结构化结果。完整链路包括视频解码、YOLO 人员检测、NMS、SORT 多目标跟踪、轨迹管理、关键帧评分与去重，以及图片和元数据上传。

我主要负责检测与跟踪链路的方案设计、施工场景数据整理和模型训练、SORT 参数调优、关键帧策略设计，以及边缘侧推理性能优化。最终输出不仅有关键帧，还包含摄像头、时间戳、track ID、人员框和置信度，便于平台检索和后续动作分析。

### 2. 为什么选择 YOLO？具体使用了哪个版本？

**参考回答：**

边缘侧同时有实时性和检测精度要求。YOLO 是单阶段检测器，可以一次前向直接完成分类和边界框回归，相比两阶段检测器更适合实时场景；同时它的部署生态比较成熟，便于导出 ONNX 或 TensorRT。

项目实际使用的是 `[YOLO 版本]`。选择该型号时，我不是只比较公开数据集精度，而是在施工验证集上比较 mAP、Recall、模型大小和端到端延迟，最终选择满足漏检要求且能稳定达到 25 FPS 的版本。这里应说明真实的 YOLO 版本和输入尺寸，不能只回答“使用了 YOLO”。

### 3. YOLO 的检测结果如何与 SORT 对接？

**参考回答：**

YOLO 每帧输出人员框、类别和置信度。我先经过置信度过滤和 NMS，再把 `[x1, y1, x2, y2, score]` 形式的检测框交给 SORT。SORT 使用卡尔曼滤波预测每条轨迹在当前帧的位置，然后计算预测框和检测框之间的 IoU 代价矩阵，最后通过匈牙利算法完成一对一匹配。

匹配成功的轨迹使用新检测结果更新；未匹配检测会创建候选轨迹；连续多帧未匹配的轨迹会被删除。工程上重点调节检测阈值、IoU 匹配阈值、`min_hits` 和 `max_age`，在误建轨迹、轨迹稳定性和遮挡恢复之间取得平衡。

### 4. SORT 的卡尔曼滤波和匈牙利算法分别解决什么问题？

**参考回答：**

卡尔曼滤波解决的是运动状态预测问题：根据目标此前的位置和速度，估计它下一帧可能出现在哪里。匈牙利算法解决的是全局最优匹配问题：在多个预测轨迹和多个检测框之间，根据 IoU 代价找到总成本最小的一对一分配。

简单来说，卡尔曼滤波负责“预测目标在哪”，匈牙利算法负责“当前检测框属于哪条轨迹”。

### 5. 为什么选择 SORT，而不是 DeepSORT 或 ByteTrack？

**参考回答：**

选择 SORT 主要考虑边缘设备算力和业务需求。施工摄像头通常相对固定，项目核心目标是稳定提取关键帧和统计短时间活动，不要求跨摄像头重识别。SORT 不需要额外的 ReID 网络，计算量小、实现简单、端侧延迟低。

它的缺点是主要依赖运动和 IoU，人员长时间遮挡或密集交叉时容易发生 ID switch。DeepSORT 通过外观特征改善遮挡后的身份关联，但会增加一次特征提取开销；ByteTrack 会利用低置信度检测框进行二次关联，对遮挡场景通常更友好。如果实际业务进一步要求长时间身份一致性，我会用施工数据对 ByteTrack 和 DeepSORT 做 IDF1、HOTA、ID switch 数量及端侧延迟的对比，而不是只看检测 mAP。

### 6. 关键帧是怎么提取的？为什么不是固定间隔抽帧？

**参考回答：**

固定间隔抽帧虽然简单，但容易重复上传静态画面，也可能错过持续时间很短的事件。项目中先利用 track ID 聚合同一人员的连续检测结果，再从检测质量、图像质量和时间覆盖三个维度给候选帧评分。

检测质量主要考虑置信度、目标框大小和遮挡程度；图像质量考虑清晰度、亮度以及人员是否处于画面边缘；时间覆盖通过冷却窗口控制同一轨迹的上传频率。一个窗口内只保留评分最高的帧，并通过时间间隔或图像相似度去重。这样能够在保留有效信息的同时减少重复图片和上传流量。

如果面试官继续追问，应说明项目真实采用了哪些评分项。没有实现图像相似度去重时，不要把它说成已上线能力，可以作为改进方案回答。

### 7. 简历中的 mAP > 90%、Recall > 90% 是怎么计算的？

**参考回答：**

mAP 和 Recall 是人员检测指标，不是对“视频摘要”整体效果的直接评价。Recall 表示所有真实人员中被模型检出的比例；AP 是某一类别在不同置信度阈值下 Precision-Recall 曲线的面积，mAP 是各类别 AP 的平均值。

因为当前任务如果只有“person”一个类别，严格来说报告的是 person AP。面试时我会明确它是 `AP@0.5` 还是 `mAP@0.5:0.95`，二者难度差异很大。该结果是在按项目、摄像头或日期隔离的测试集上计算，避免同一段视频的相邻帧同时进入训练集和测试集造成数据泄漏。

更严谨的简历写法是：

> 人员检测 `[AP@0.5 或 mAP@0.5:0.95]` > 90%、Recall > 90%；关键帧有效率 `[真实指标]`；在 `[设备型号]`、`[输入分辨率]` 下端到端处理速度 ≥ 25 FPS。

关键帧策略本身应使用人工抽检有效率、关键事件覆盖率、重复率和上传压缩比评价，不能只用检测 mAP 代替。

### 8. 25 FPS 是如何测试的？包括视频解码和后处理吗？

**参考回答：**

我会先明确测试条件：设备是 `[设备型号]`，模型是 `[YOLO 版本]`，输入分辨率是 `[输入分辨率]`，Batch Size 为 1，并区分纯模型 FPS 和端到端 FPS。简历中的 25 FPS 应尽量使用端到端口径，即包含视频解码、图像预处理、YOLO 推理、NMS、SORT 更新和关键帧判断；网络上传采用异步队列，不阻塞推理主循环。

性能优化主要包括选择轻量模型、固定输入尺寸、导出 ONNX/TensorRT、使用 FP16 或经过校准的 INT8、复用内存、减少 CPU 与 GPU 之间的拷贝，以及让解码、推理和上传通过有界队列并行执行。优化后还要同时检查准确率，特别是 INT8 对小目标人员 Recall 的影响。

### 9. 遮挡、人员交叉导致 ID 变化时怎么处理？

**参考回答：**

短时遮挡时，SORT 可以依靠卡尔曼预测将轨迹保留 `max_age` 帧，目标重新出现且位置匹配时继续原轨迹。对于长时间遮挡或多人交叉，纯 IoU 匹配的能力有限，可能产生 ID switch。

在当前业务里，我首先通过调整 `max_age`、匹配阈值和检测 Recall 降低问题，并且关键帧上传不把 track ID 当作人员真实身份，只把它作为单摄像头短时间窗口内的轨迹标识，因此偶发 ID switch 对业务影响可控。如果业务需要准确统计个人级时长，就必须增加外观特征、改用 ByteTrack/DeepSORT，或者通过轨迹后处理进行合并。

### 10. 如何证明这个方案确实降低了成本？

**参考回答：**

应该比较改造前后的可量化指标，包括每小时原始视频大小、每小时上传关键帧数量与大小、带宽峰值、平台侧解码和推理任务量，以及关键事件覆盖率。上传压缩比可以定义为：

`1 - 关键帧及元数据上传量 / 原始视频上传量`。

只有在关键事件覆盖率满足业务要求的前提下，传输量和平台计算量的下降才有意义。面试时应给出项目真实统计结果；如果只测过 FPS，而没有统计成本下降比例，应明确说明目前通过数据量变化进行估算，不把估算说成线上实测。

## 二、ResNet3D 动作识别与进度分析面试问答

### 11. 为什么目标检测之后还需要 ResNet3D？整体流程是什么？

**参考回答：**

YOLO 和 SORT 只能回答“画面中有哪些人、人员在哪里以及轨迹如何变化”，无法仅凭位置可靠判断人员正在执行什么施工动作。ResNet3D 同时在空间和时间维度提取特征，可以利用连续帧中的运动信息进行动作分类。

具体流程是：根据 SORT 的 track ID 从连续帧中裁剪同一人员区域，组成长度为 `T` 的视频 clip；对 clip 做尺寸归一化和时序采样，再输入 ResNet3D 得到 `[动作类别]` 的概率；最后通过滑动窗口、概率平滑和状态机将 clip 级预测合并为动作片段，结合视频时间戳计算每类动作的持续时间，并映射到施工进度规则。

### 12. ResNet3D 和普通 ResNet2D 有什么区别？为什么不用单帧分类？

**参考回答：**

ResNet2D 的卷积核只在图像的高和宽两个维度移动，主要提取单帧外观；ResNet3D 的卷积核还包含时间维度，可以联合学习外观和运动变化。

施工动作经常存在单帧外观相似但动态过程不同的情况，例如“拿着工具站立”和“持续操作工具”，单帧很难区分。ResNet3D 可以利用多帧运动信息，链路也比“2D CNN + RNN”更统一。它的代价是计算量和显存占用更高，因此要在 clip 长度、采样步长和输入尺寸之间做权衡。

### 13. ResNet3D 的输入是什么？如何保证同一个 clip 是同一个人？

**参考回答：**

模型输入一般是 `[C, T, H, W]`，其中 `T` 是连续或等间隔采样的帧数。项目中利用 SORT 的 track ID 建立人员级 clip buffer，将同一轨迹对应的人员框按时间顺序裁剪并缓存；裁剪时会适当扩大边界框，保留工具和周围施工对象等上下文。

如果轨迹中间短暂丢失，可以使用卡尔曼预测位置或复制邻近有效帧补齐，但连续缺失过长时会丢弃该 clip，避免错误轨迹污染动作识别。这里需要特别关注 ID switch，因为把两个人的画面拼进同一个 clip 会直接造成错误标签。

### 14. 动作训练数据如何标注和划分？

**参考回答：**

动作标注以时间片段为单位，包括动作类别、开始时间、结束时间，并与人员轨迹关联；然后按固定窗口切成训练 clip。边界附近容易同时包含前后两个动作，我会设置有效动作占比阈值，低于阈值的 clip 丢弃或标为过渡状态，而不是强行赋予单一标签。

数据集不能随机按帧或 clip 划分，因为同一视频的相邻片段高度相似，会造成数据泄漏。更合理的方式是按项目、摄像头、施工区域或日期分组划分训练集、验证集和测试集，以验证模型对新场景的泛化能力。对于长尾动作，可以采用类别均衡采样、类别权重或补充困难样本。

### 15. 如何把 clip 级动作分类转换为动作持续时长？

**参考回答：**

模型对滑动窗口中的每个 clip 输出类别概率。我先对连续窗口做概率平滑，例如移动平均或多数投票，再通过状态机设置动作进入阈值、退出阈值和最短持续时间，减少类别在相邻帧之间频繁跳变。

确认动作区间后，使用原始视频时间戳计算 `结束时间 - 开始时间`，而不是简单用帧数除以标称 FPS，因为网络流可能存在丢帧或帧率波动。短时轨迹丢失可以在允许间隔内连接，超过阈值则结束当前动作片段。最后按人员、区域和任务聚合动作时长，并根据业务规则换算施工进度。

### 16. “施工进度平均准确率 ≥ 95%”具体指什么？这个指标可靠吗？

**参考回答：**

“平均准确率”表述不够明确，面试时必须说明它究竟是 clip 级动作分类 Accuracy、各动作类别平均准确率，还是最终进度判断的 Accuracy。如果类别分布不均衡，只看总体 Accuracy 可能被高频类别掩盖，例如“非施工”占比很高时，即使少数动作识别很差，总体 Accuracy 也可能很高。

我会同时报告混淆矩阵、每类 Precision/Recall/F1 和 Macro-F1；对于时序动作片段，还应评估片段级 F1 或不同时间 IoU 阈值下的 mAP；对于动作时长，则报告 MAE 或相对误差。95% 必须对应独立测试集上的明确口径，并说明样本规模和划分方式。

更严谨的简历写法是：

> 在按 `[项目/摄像头/日期]` 隔离的测试集上，`[clip 级 Accuracy / Macro-F1]` 达到 `[真实数值]`，动作时长 MAE 为 `[真实数值]`，最终进度判断准确率为 `[真实数值]`。

### 17. 检测和跟踪错误会怎样影响动作识别？如何降低误差传递？

**参考回答：**

这是一条级联系统，上游漏检会造成 clip 缺帧，框抖动会引入背景变化，ID switch 会把不同人员拼接在一起，这些问题都会降低动作识别效果。因此不能只用人工裁剪的完美框评估 ResNet3D，还要使用 YOLO + SORT 的真实输出做端到端测试。

具体措施包括适当扩展人员框、对框坐标做时序平滑、过滤过小或缺失比例过高的 clip、给短时缺帧设置容错、将低置信度动作标为 unknown，以及使用真实跟踪框生成一部分训练数据，让模型适应检测框偏移。评估时分别报告理想框和真实跟踪框下的结果，可以定位性能损失来自哪一层。

### 18. 这个方案最容易失败的场景有哪些？如何改进？

**参考回答：**

主要失败场景包括人员密集遮挡、远距离小目标、夜间或强光、摄像头抖动、相似动作、动作持续时间很短，以及工具或身体关键部位被遮挡。

对应的改进方向包括补充困难场景数据和针对性增强；提升输入分辨率或加入小目标检测层；使用 ByteTrack/DeepSORT 改善轨迹一致性；在动作模型中加入人体关键点、光流或工具交互特征；使用更强的时序模型；对低置信度结果设置人工复核或 unknown 类。工程上应优先根据线上错误分布选择改进项，而不是直接替换成更大的模型。

### 19. 如果让你重新设计这套系统，你会重点改什么？

**参考回答：**

我会先统一离线和线上评估体系，把检测 AP/Recall、跟踪 IDF1/HOTA、动作 Macro-F1、时长 MAE、关键事件覆盖率、重复率、上传压缩比和端到端延迟分别量化，避免只用一个 Accuracy 描述整条链路。

模型方面，我会在相同设备和数据集上对 SORT 与 ByteTrack 做对照实验，并针对密集遮挡场景决定是否加入轻量 ReID；动作识别方面会增加 unknown/过渡状态和时序后处理。系统方面则加强异步流水线、断网缓存、失败重传、监控和模型版本管理，使端侧系统不仅“能跑”，而且可以长期稳定运行。
