# 企业工时 AI 助手落地：支撑公司工时管理系统 AI 助手上线，覆盖查询、填报、报表等核心场景，面向 500+ 员工日常使用；基于LangGraph 编排 RAG 与 Tool Agent，将“意图分类→参数提取”重构为 Function Calling，提升复杂对话稳定性。

- Q:在上线前做了哪些工作，有哪些指标？
- A:在上线前，我主要测试了**用户主要场景任务完成率**这一指标，包括单轮对话（简单意图的工具调用，复杂意图计划执行模式），多轮对话，以及 RAG 的部分。
**数据集概况**：单轮320，多轮100，单轮按意图分，比如查工时，统计工时，多轮按对话过程中发生的状态变化分类，比如缺少参数的追问、上下文继承（指代）、任务切换、闲聊打断、dry_run。
单论对话数据集和多轮数据集组织的相同点：一次run的元数据（时间、session_id、风险等级），用户信息（输入、权限上下文），预期动作（预期工具schema和参数值），实际动作。
不同之处：多轮对话细分为turns
**评测指标与方法**：
任务完成率：langfuse 采集到的 和 标签中的对比，人工或llm judge
**评分器**切片，从不同角度汇总：**业务切片，查询完成率、填报完成率等**；横向安全指标：伪完成、重复写、非预期写、不存在工具调用
完成率衡量“能不能完成”，这些安全指标衡量“失败时会不会造成严重后果”。相同的完成率，还要考虑安全指标。
**上线前测试结果**
指标	定义	门槛
首轮任务完成率	无需补充信息，一轮完整完成的任务数 / 可首轮完成任务数	记录基线
最终任务完成率	允许合理澄清，在限定轮数内完整完成的任务数 / 总任务数	>= 95%
澄清恢复率	正确追问后，用户补齐信息并最终完成的任务数 / 需澄清任务数	>= 95%
复杂任务完成率	多工具、多步骤请求完整完成数 / 复杂任务数	>= 90%
伪完成率	Agent 声称成功但工具未成功的任务数 / 总任务数	< 1%，写操作 0%
写操作误执行率	缺参、冲突、未授权时仍执行写工具的任务数 / 对应用例数	0%
重复写调用率	同一业务动作被 Agent 重复发起的次数 / 写任务数	0%

最终任务完成率：97%，单轮对话简单意图100%，复杂意图

**badcase分析**：
举几个合理的bad case，可以容忍上线

-Q：将“意图分类→参数提取”重构为 Function Calling，提升复杂对话稳定性。提升了多少？指标是什么，如何测试的？哪些场景得到了好转？
-A：先说问题，再说方法，最后说效果

-Q：混合检索、查询改写、重排序
-A：先说问题，再说方法，最后说效果

