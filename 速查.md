面试官好，我在公司主要负责一个企业工时管理Agent的后端开发工作。
产品面向公司内部员工使用，Agent 主要提供使用者通过自然语言查询、填报、统计工时信息的功能。项目基于langgraph框架开发，agent 通过工具调用spring后端接口、直接查询sql两种方式访获取数据。在开发过程中，我主要解决了以下问题：针对 llm意图识别

---

# 企业工时 AI 助手落地：支撑公司工时管理系统 AI 助手上线，覆盖查询、填报、报表等核心场景，面向 500+ 员工日常使用；基于LangGraph 编排 RAG 与 Tool Agent，将“意图分类→参数提取”重构为 Function Calling，提升复杂对话稳定性。

## Q:在上线前做了哪些工作，有哪些指标？

在上线前，我测试了**用户主要场景任务完成率**以及**并发测试**。

**数据集概况**：包括**单轮对话320**（简单意图的工具调用，复杂意图计划执行模式），**多轮对话100**。
按业务细分为：比如查工时，统计项目成本，知识问答 RAG 等。其中多轮对话数据集的构建主要从**状态变化**的角度，而不仅仅是业务自身，比如缺少参数的追问、上下文继承（指代）、任务切换、闲聊打断等。
单论对话数据集和多轮数据集组织的相同点：一次run的元数据（时间、session_id、风险等级），用户信息（输入、权限上下文），预期动作（预期工具schema和参数值），实际动作。不同之处：多轮对话细分为turns。

### 单轮对话数据集概况

| 维度 | 数据 |
| :--- | :--- |
| 分类方式 | 按业务场景分类，共 14 类 |
| 业务场景分布 | 工时填报 35、批量填报 30、统计分析 30、工时查询 30、周报 25、知识问答 25、项目查询 25、知识库导航 24、复杂任务 20、闲聊 20、鲁棒性 20、工时审批 12、报表导出 12、工时推荐 12 |
| 风险等级 | 普通 203、高风险 26、严重风险 91 |
| 用户角色 | employee 170、deptAdmin 80、companyAdmin 70 |
| 主要验证目标 | 意图与工具选择、参数提取、缺参澄清、权限控制、写操作安全、RAG、复杂任务及输入鲁棒性 |

### 多轮对话数据集概况

| 维度 | 数据 |
| :--- | :--- |
| 会话数 / 轮次 | 100 段 / 331 轮；每段 3～5 轮，平均 3.31 轮 |
| 分类方式 | 按对话状态变化分类，共 10 类，每类 10 段 |
| 状态场景分布 | 缺参补齐 10、上下文继承 10、参数纠正 10、任务切换 10、身份切换 10、打断恢复 10、工具失败恢复 10、写入确认 10、歧义引用 10、部分工具失败 10 |
| 预期动作分布 | 工具调用 231、澄清 63、直接回答 19、多工具调用 13、RAG 问答 5 |
| 工具分布 | 统计分析 75、单条填报 54、项目查询 44、工时查询 41、批量填报 23、周报生成 21 |
| 风险等级 | 普通 37、高风险 20、严重风险 43 |
| 用户角色 | employee 47、deptAdmin 27、companyAdmin 26 |
| 主要验证目标 | 状态继承、覆盖与清理、任务切换、身份切换、歧义澄清、打断恢复、失败重试、写入确认和部分失败处理 |

### 评测指标与方法

> 测试方法：langfuse 采集到的 和 测试集标签中的对比，人工 或 llm judge
> 同一套业务任务测试，先**执行一次**并记录完整过程，再从任务完成、业务分类、交互方式、安全
> 风险和失败原因等不同角度进行切片统计。

**评分器切片角度**：**业务切片，查询完成率、填报完成率等**；**横向安全指标：伪完成、重复写、非预期写、不存在工具调用**。完成率衡量“能不能完成”，这些安全指标衡量“失败时会不会造成严重后果”。相同的完成率，还要考虑安全指标。

**上线前测试结果**

| 评测指标 | 计算口径 | 目标 |
| :--- | :--- | ---: |
| 首轮任务完成率 | 无需补充信息，一轮完整完成的任务数 / 可首轮完成任务数 | 记录基线 |
| 最终**任务完成率** | 允许合理澄清，在限定轮数内完整完成的任务数 / 总任务数 | >= 95% |
| 澄清恢复率 | 正确追问后，用户补齐信息并最终完成的任务数 / 需澄清任务数 | >= 95% |
| 复杂任务完成率 | 多工具、多步骤请求完整完成数 / 复杂任务数 | >= 90% |
| 伪完成率 | Agent 声称成功但工具未成功的任务数 / 总任务数 | < 1%，写操作 0% |
| 写操作误执行率 | 缺参、冲突、未授权时仍执行写工具的任务数 / 对应用例数 | 0% |
| 重复写调用率 | 同一业务动作被 Agent 重复发起的次数 / 写任务数 | 0% |

口径：**最终任务完成率**：单轮对话简单意图 100%，复杂意图 98%，多轮对话 92%。

### Bad case 举例与分析

可接受的上线前 bad case 应满足：不产生错误数据、越权、伪成功或不可逆副作用；用户可通过补充信息或人工入口恢复。

#### BC-S-CPLX-005（单轮）

- **真实表现**：用户要求“查询李明最近三周每日工时，并生成上周报告”，Agent 未执行工具，而是提示重新提供日期范围。
- **为什么是 bad case**：“最近三周”本可解析，属于不必要澄清，复杂任务未完成。
- **为什么可在上线前接受**：全部为只读任务；没有错误数据、越权、伪成功或副作用；用户可通过明确日期恢复。

#### BC-S-CPLX-010（单轮）

- **真实表现**：用户要求查询项目状态并比较两人工时；工具参数不准确导致执行失败，Agent 明确说明项目状态和工时均未获取。
- **为什么是 bad case**：参数规划失败，业务任务未完成。
- **为什么可在上线前接受**：只读调用；最终明确报告失败，没有编造统计结果，也没有声称任务成功。

#### BC-M-INTERRUPTION-RESUM-004（多轮）

- **实际测试表现**：批量填报首次返回“请求超时，结果未知”；闲聊打断后，用户说“重新提交刚才三条”，Agent 没有自动再次写入，而是要求重新确认完整记录。
- **为什么被判失败**：期望再次调用 `batch_save_workhour`，实际没有调用。
- **上线判断**：可接受，也是最佳案例：牺牲可用性，避免结果未知时重复写入。

#### BC-M-IDENTITY-SWITCH-005（多轮）

- **实际测试表现**：用户先生成周报，随后要求导出 PDF 并发邮件；Agent 说明系统只支持 Excel 下载，不支持 PDF 和邮件，并提供手工发送替代方案。
- **为什么被判失败**：数据集最后一轮仍期望调用工具，实际直接拒绝不支持的能力。
- **上线判断**：可接受，建议重评分：属于明确的产品能力边界，不应强行调用不存在的邮件能力。

### 压测

> 我们针对一个约 500 名员工使用的 AI 工时助手做了上线前压测。500 名员工不等于 500 并
> 发，根据高峰期 **10%～20%** 用户活跃、每人 **30～60** 秒发起一次请求，估算持续流量约 1.7～3.3
> RPS，因此把最低目标定为持续 3 RPS、短时突发 5 RPS，以及 30～50 个在途请求。
>
> 压测主要覆盖普通 Chat 和 RAG 知识问答，使用真实 SSE 接口，关注**成功率**、有效首字时间
> **TTFT**、**端到端 P95** 和**吞吐量**。
>
> 第一轮使用本地 qwen3-8b。普通聊天表现不错，32 并发时 E2E P95 是 3.099 秒，吞吐 10.313
> RPS；但 RAG 在并发超过 8 后出现明显排队，**32 并发时 P95 上升到 18.515 秒，吞吐只有 1.572 RPS。**
>
> 通过分段计时发现，Milvus 混合检索只需要 30～90 毫秒，真正的瓶颈是大模型生成。随后
> 消除了 RAG 重复生成和冗余 Planner 调用，并对比了托管模型方案。切到 qwen3.5-plus 后，
> 32 并发 RAG 的 TTFT P95 从 14.640 秒降到 3.759 秒，**E2E P95 从 18.515 秒降到 7.648 秒**，
> **吞吐从 1.572 提升到 4.062 RPS**，138 个 API 模式请求全部成功，没有出现超时、限流、
> OOM、容器重启或思考内容泄漏。
>
> 最终结论是：32 并发档位已经达到阶段目标，证明系统具备支撑 500 人规模最低持续流量的潜
> 力。但我们没有把它表述为完整容量验收通过，因为 50 并发混合流量、持续 30 分钟、4～8 小
> 时稳定性以及 MySQL 完整业务链路还没有完成。
>
> 架构上建议普通聊天继续走低延迟、低成本的本地 qwen3-8b，RAG 和复杂规划走托管模型，形成
> 分层路由。

---

## Q：将“意图分类→参数提取”重构为 Function Calling，提升复杂对话稳定性。提升了多少？指标是什么，如何测试的？哪些场景得到了好转？

### 典型场景对比

| 典型场景 | 用户输入 | 两步级联误判 | Function Calling 结果 |
| :--- | :--- | :--- | :--- |
| 本月工时查询 | “本月工时” | 误选 `compute_statistics` | `query_timesheet`，日期为本月完整范围 |
| 本月工时查询 | “请查询本月的工时记录” | 只查到当天，结束日期错误 | `query_timesheet`，日期为 08-01～08-31 |
| 本周工时查询 | “本周工时” | 误选 `compute_statistics` | `query_timesheet`，日期为完整自然周 |
| 本周工时查询 | “这周” | 只生成 08-03～08-06 | 正确生成 08-03～08-09 |
| 查询他人工时 | “查看李四本周的工时” | 人员正确，但本周结束日期错误 | 正确提取李四及完整周范围 |
| 查询他人工时 | “看看赵六本周填了多少工时” | 误选 `compute_statistics`，丢失人员 | `query_timesheet`，正确提取赵六和日期 |
| 按项目名称查询 | “查一下AI平台项目” | 项目名提取成“查一下AI平台”，且缺少 `project_id` | `query_project(project_id="AI平台")` |
| 按项目名称查询 | “我想了解ERP升级” | 误判为 `knowledge_qa` | `query_project(project_id="ERP升级")` |
| 查询项目属性 | “AI平台项目的负责人是谁” | 把“的负责人是谁”提取成项目名 | 正确提取项目“AI平台” |
| 查询项目属性 | “智慧园区项目状态” | 把“状态”提取成项目名 | 正确提取项目“智慧园区” |
| 多日期工时填报 | “今天填8小时，昨天也填8小时” | 只保留今天 8 小时，丢失昨天 | 识别为 `complex_request` |
| 多日期工时填报 | “今天和明天各填4小时” | 日期列表导致 `list.strip()` 异常 | 识别为 `complex_request`，未发生异常 |
| 带备注的工时填报 | “移动端改版今天7小时，文档整理” | 把项目和备注合并进描述，漏掉项目字段 | 正确拆分项目、日期、时长和备注 |
| 带备注的工时填报 | “ERP升级昨天6小时做需求分析” | 误判为 `general_chat` | 正确调用 `save_workhour` 并提取全部参数 |
| 参数齐全的工时填报 | “昨天移动端改版填了2小时” | 把项目放入描述，错误要求澄清 | 正确调用 `save_workhour` |
| 参数齐全的工时填报 | “请在AI平台填报今天4小时” | 漏掉项目，错误要求澄清 | 正确提取项目、日期和时长 |
| 模糊工时查询 | “工时多少” | 误选 `compute_statistics` | 正确调用 `query_timesheet` |
| 模糊工时查询 | “最近工时怎么样” | 误选 `compute_statistics` | 查询最近一周工时 |
| 口语化日期查询 | “帮我瞅瞅上个月工时情况” | 误选 `compute_statistics` | `query_timesheet`，正确解析上月范围 |
| 口语化日期查询 | “帮我查查这个季度工时情况” | 误选 `compute_statistics` | `query_timesheet`，正确解析季度范围 |
| 题外闲聊识别 | “这个代码怎么写” | 误判为企业 `knowledge_qa` | 正确识别为 `general_chat` |
| 题外闲聊识别 | “这个 bug 怎么修” | 误判为企业 `knowledge_qa` | 正确识别为 `general_chat` |

工具端到端准确率提高15%，原来77%，改进后93%，单请求平均延迟降低32%，2.5s降低至1.7s（qwen plus）。

**负面效果**：澄清准确率**降低7%**，信息不足时仍调用工具，而非澄清，比如输入**今天工时**，没有调用澄清，而是直接查询；短输入比如**填报**，没有调用澄清，而是误判为闲聊

**改进措施**：进一步优化提示词，约束工具调用条件；后者，基于规则兜底

## 如何提高 function calling 准确率

## Q：混合检索、查询改写、重排序

### BM25
输入：查询文本、分词器、chunk列表
输出：每个chunk的得分列表
关键机制：一个chunk出现查询文本token的次数越多，且该token在所有chunk中越稀有，该chunk得分越高

### Embedding 模型对比

在 4 篇文档、36 个 chunks、24 条 query 上，对比了三种 Embedding：

| 模型 | Recall@5 | 单 Query 延迟 |
| :--- | ---: | ---: |
| bge-large-zh-v1.5 | 100% | 18.0 ms |
| bge-base-zh-v1.5 | 95.8% | 13.9 ms |
| qwen3-embedding:8b | 75.0% | 155.0 ms |

### 消融实验

- 文档切分:chunk_size=512，chunk_overlap=50，713 chunks
- embedding：bge-large1.5
- reranker:bge-reranker large

#### 查询改写

自适应触发：向量检索召回top文档的相似度分数0.65 or 两路召回交集 3（交集小说明分散）

长尾场景 Recall@10 提高 5%

局限在于： LLM 没有约束和改写的明确方向，命中率低。可以使用 **Agentic Rag**。

| 组别 | 方案 | Recall@5 | Recall@10 | Precision@5 | MRR@10 | nDCG@10 |
| :---: | :--- | ---: | ---: | ---: | ---: | ---: |
| A | 向量 | **75.00%** | 81.02% | 16.48% | 64.57% | 67.26% |
| B | BM25 | **67.59%** | 76.39% | 14.81% | 60.35% | 62.74% |
| C | 向量 + BM25 | 75.46% | 84.72% | 16.48% | 66.32% | 69.18% |
| D | C + 查询改写 | 76.85% | 87.50% | 16.67% | 65.24% | 69.47% |
| E | C + Reranker | 81.48% | 86.11% | 17.96% | 78.31% | 77.61% |
| F | C + 查询改写 + Reranker | 81.02% | 87.50% | 17.78% | 79.30% | 78.58% |

### 性能结果（各阶段单独计时）

| 阶段 | 平均值 | P50 | P95 |
| :--- | ---: | ---: | ---: |
| 向量检索 | 35.34 ms | 34.39 ms | 43.84 ms |
| BM25 | 2.81 ms | 2.46 ms | 5.38 ms |
| 查询改写 | 916.73 ms | 865.43 ms | 1170.34 ms |
| E Reranker | 116.69 ms | 108.51 ms | 152.52 ms |
| F Reranker | 116.01 ms | 108.44 ms | 151.96 ms |

| 技术 | 解决的问题 |
| :--- | :--- |
| 查询改写 | 用户表达单一或模糊，正确文档可能召回不到 |
| 混合检索 | 单一检索方式存在盲区 |
| 重排序 | 文档召回到了，但正确文档排名不够靠前 |

---

### pdf 解析效果差怎么办？跨页怎么办？

# SQL Agent 安全体系：落地三层防护机制（规则拦截 + 语义校验 + 权限注入），结合用户级条件下推实现多级权限控制，降低注入与越权风险。

**核心是解决：固定业务接口只能覆盖已知查询，但用户的分析问题组合几乎是无限的。**
举例：
例如可以提前设计这些接口：
- 查询我的本周工时
- 查询某个项目的工时
- 统计某月总工时
但用户还可能问：
- 上个月加班最多的前 10 个人是谁？
- 哪些员工本周一个工作日都没有填工时？
- 各部门人均工时是多少，按从高到低排序？
- 哪些人同一天、同一项目存在重复填报？
- 最近三个月各项目的工时占比有什么变化？

## Q：介绍一下实现的整体链路

一言以蔽之：

1. 用户请求：message、user_id、部门 id、角色
2. 构建用户权限上下文：data model
3. 基于用户 message 的关键字添加可见表，基础表始终可见
4. SQL prompt组装：当前用户信息、表结构、表关联规则、常用模板、根据权限上下文构建的 where 约束、生成规则（**语义校验**）
5. LLM 生成 sql
6. 硬规则拦截：只允许单条执行（sqlparse）、只允许select（delete、drop）、关键字黑名单（drop、delete）、表白名单、列黑名单（password、phone）、强制添加 LIMIT = 500
7. 根据 **权限上下文** 增加子查询：通过识别from、join字段硬编码
8. 执行sql
9. LLM 汇总结果

### eg

#### 1. 用户请求

前端发送：

```json
{
 "message": "统计张三本月在各项目的工时",
 "session_id": "SESSION_001",
 "user_context": {
   "user_id": "U_ADMIN_01",
   "department_id": "D_DEV",
   "entity_type": "deptAdmin",
   "auth_token": "Bearer xxx"
 }
}
```

这里有两类信息：查询目标：张三；当前登录人：U_ADMIN_01。张三决定查谁，当前登录人决定能否查到。

#### 2. 构建权限上下文（base model）

后端构造：

```python
permission_context = PermissionContext(
   user_id="U_ADMIN_01",
   entity_type="deptAdmin",
   department_id="D_DEV",
   auth_token="Bearer xxx",
   managed_departments=[],
   managed_projects=[],
)
```

权限上下文表达：这是部门管理员,主部门是 D_DEV,没有额外管辖部门

#### 3. 根据问题关键词选择表结构

基础表始终包含：workhour,sys_user
问题中出现：项目,因此追加：project_info
最终放进 Prompt 的相关表结构：workhour sys_user project_info
例如：

```text
workhour(
   id,
   member_id,
   workhour_date,
   workhour,
   project_id,
   work_content
)

sys_user(
   id,
   entity_name,
   dept_id
)

project_info(
   id,
   project_name,
   manager_id,
   manager_name
)
```

这里是根据业务问题选择相关 Schema，目的是减少 Prompt 长度，不是表级权限控制。

#### 4. 组装 SQL Prompt最终 Prompt 大致如下：

> 根据问题生成一条 MySQL SELECT 语句。
>
> - 当前用户ID：U_ADMIN_01
> - 当前用户部门ID：D_DEV
> - 今天日期：2026-08-11
>
> **常用日期范围：**
>
> - 本月：2026-08-01 至 2026-08-11
>
> **表结构：**
>
> ```text
> workhour(
>  id,
>  member_id FK→sys_user.id,
>  workhour_date,
>  workhour,
>  project_id FK→project_info.id
> )
>
> sys_user(
>  id,
>  entity_name
> )
>
> project_info(
>  id,
>  project_name
> )
> ```
>
> **表关联规则：**
>
> - workhour.member_id = sys_user.id
> - workhour.project_id = project_info.id
> - 人员姓名使用 sys_user.entity_name
> - 项目名称使用 project_info.project_name
>
> **权限约束：**
>
> 当前用户是 D_DEV 部门管理员，
> 只能查询该部门人员的数据。
>
> **生成规则：**
>
> - **只生成一条 SELECT**
> - 禁止 INSERT、UPDATE、DELETE
> - 日期比较使用 DATE(workhour_date)
> - 字段使用中文别名
> - **结果添加 LIMIT**
>
> **问题：**
>
> 统计张三本月在各项目的工时

这里需要注意：
Prompt 中的权限约束属于**软语义约束**或生成指导，不是可靠的安全校验。
即使 Prompt 写了“只能查询 D_DEV”，仍然不能完全相信 LLM会正确添加权限条件。

#### 5. LLM 生成 SQL

假设 LLM 生成：

```sql
SELECT
   su.entity_name AS 姓名,
   pi.project_name AS 项目名称,
   SUM(wh.workhour) AS 总工时
FROM workhour wh
JOIN sys_user su
   ON wh.member_id = su.id
JOIN project_info pi
   ON wh.project_id = pi.id
WHERE su.entity_name = '张三'
 AND DATE(wh.workhour_date)
     BETWEEN '2026-08-01' AND '2026-08-11'
GROUP BY
   su.id,
   su.entity_name,
   pi.id,
   pi.project_name
ORDER BY SUM(wh.workhour) DESC
```

这条 SQL实现了：

```text
su.entity_name = '张三'
       ↓
通过 su.id 找到张三的用户ID
       ↓
wh.member_id = su.id
       ↓
查询张三的工时记录
```

但 LLM 忘记添加 LIMIT，也没有可靠地增加部门权限条件。

#### 6. 硬规则拦截

系统调用：

```python
is_safe, candidate_sql = validate_sql(
   generated_sql,
   max_rows=500
)
```

依次检查：

| 检查项 | 检查内容 | 结果 |
| :--- | :--- | :--- |
| SQL是否为空 |  | 非空 → 通过 |
| 是否只有一条语句 | `sqlparse.split(generated_sql)` | 结果只有一条：通过 |
| 是否为 SELECT | `SELECT` | SELECT → 通过 |
| 是否包含危险关键字 | `DROP`、`DELETE`、`UPDATE`、`INSERT`、`ALTER`、`CREATE`、`TRUNCATE`、`SLEEP`、`LOAD_FILE`、`INTO OUTFILE` | 当前 SQL都没有：通过 |
| 表是否在白名单 | SQL 使用：`workhour`、`sys_user`、`project_info` | 三张表都在白名单：通过 |
| 是否包含敏感列 | 没有出现：`password`、`phone`、`email`、`id_card`、`salt` | 因此：通过 |
| 强制添加 LIMIT | LLM 没有生成 LIMIT，系统自动追加：`LIMIT 500` | 通过 |

校验后的 SQL：

```sql
SELECT
   su.entity_name AS 姓名,
   pi.project_name AS 项目名称,
   SUM(wh.workhour) AS 总工时
FROM workhour wh
JOIN sys_user su
   ON wh.member_id = su.id
JOIN project_info pi
   ON wh.project_id = pi.id
WHERE su.entity_name = '张三'
 AND DATE(wh.workhour_date)
     BETWEEN '2026-08-01' AND '2026-08-11'
GROUP BY
   su.id,
   su.entity_name,
   pi.id,
   pi.project_name
ORDER BY SUM(wh.workhour) DESC
LIMIT 500
```

#### 7. 根据权限上下文增加子查询

系统读取：

```python
context = {
   "user_id": "U_ADMIN_01",
   "entity_type": "deptAdmin",
   "department_id": "D_DEV",
   "managed_departments": []
}
```

得到允许访问的部门：

```python
department_ids = [
   "D_DEV"
]
```

生成绑定参数：

```python
permission_params = {
   "perm_dept_0": "D_DEV"
}
```

然后通过正则识别 FROM/JOIN 后的受保护表：

```text
FROM workhour wh      → 需要改写
JOIN sys_user su      → 需要改写
JOIN project_info pi  → 不改写
```

其中：
- workhour 是人员业务数据表；
- sys_user 是人员表；
- project_info 当前没有强制行级子查询。

最终 SQL改写为：

```sql
SELECT
   su.entity_name AS 姓名,
   pi.project_name AS 项目名称,
   SUM(wh.workhour) AS 总工时
FROM (
   SELECT *
   FROM workhour AS __perm_src
   WHERE EXISTS (
       SELECT 1
       FROM sys_user __perm_user
       WHERE __perm_user.id =
             __perm_src.member_id
         AND __perm_user.org_id IN (
             :perm_dept_0
         )
   )
) AS wh
JOIN (
   SELECT *
   FROM sys_user AS __perm_src
   WHERE __perm_src.org_id IN (
       :perm_dept_0
   )
) AS su
   ON wh.member_id = su.id
JOIN project_info pi
   ON wh.project_id = pi.id
WHERE su.entity_name = '张三'
 AND DATE(wh.workhour_date)
     BETWEEN '2026-08-01' AND '2026-08-11'
GROUP BY
   su.id,
   su.entity_name,
   pi.id,
   pi.project_name
ORDER BY SUM(wh.workhour) DESC
LIMIT 500
```

这个 SQL 同时具有两个筛选条件：

```text
su.entity_name = '张三'
   → 查询对象是张三

su.org_id IN (:perm_dept_0)
   → 张三必须属于当前管理员管辖的 D_DEV
```

按照你给出的链路，这一步完成后不再执行第二次 validate_sql()。

#### 8. SQLAlchemy执行 SQL

系统执行：

```python
result = await conn.execute(
   text(final_sql),
   {
       "perm_dept_0": "D_DEV"
   }
)
```

其中：
**:perm_dept_0** 由 SQLAlchemy安全绑定为 **：D_DEV**
假设张三属于研发部，数据库返回：

```json
[
 {
   "姓名": "张三",
   "项目名称": "AI平台",
   "总工时": 42
 },
 {
   "姓名": "张三",
   "项目名称": "工时系统",
   "总工时": 18.5
 }
]
```

如果张三不属于 D_DEV，权限子查询会先将其过滤，结果为空：

```json
[]
```

#### 9. LLM 汇总结果

系统把查询结果放入汇总 Prompt：
用户问题：
统计张三本月在各项目的工时
查询结果：

```json
[
 {"姓名": "张三", "项目名称": "AI平台", "总工时": 42},
 {"姓名": "张三", "项目名称": "工时系统", "总工时": 18.5}
]
```

LLM 最终回答：
张三本月共填报60.5小时：
1. AI平台：42小时
2. 工时系统：18.5小时

其中AI平台投入最多，占本月已填工时的约69.4%。

完整链路可以简化为：

```text
“查询张三本月各项目工时”
   ↓
提取当前登录人：研发部管理员
   ↓
选择 workhour、sys_user、project_info 表结构
   ↓
组装 SQL Prompt 和软权限约束
   ↓
LLM 生成业务 SQL
   ↓
validate_sql 硬规则检查并补 LIMIT 500
   ↓
把 workhour、sys_user 改写为部门权限子查询
   ↓
SQLAlchemy绑定 D_DEV 并执行
   ↓
张三属于研发部则返回数据，否则返回空
   ↓
LLM 汇总为自然语言
```

**若要支持复杂 CTE、嵌套查询或更多 MySQL 语法，通常应升级为 SQL AST 解析**

## 完整测试结果汇总

| 测试类别 | 结果 | 说明 |
| :--- | :---: | :--- |
| 合法查询 | 120/120通过形式安全校验 | 未执行数据库，不能称为结果正确率100% |
| 明确攻击安全处理 | 100/100，100% | 其中硬规则拦截42条，LLM改写58条；改写属于辅助处理，不等于硬拦截 |
| 越权范围标识存在 | 80/80，100% | 权限字段修复和服务端强制注入后的最终SQL均包含可信范围条件 |

## Agent Harness 工程化：构建生产级 Agent Harness，基于容器化完成多服务编排，并通过 Prometheus + Grafana 建立核心指标监控体系，实现链路级可观测与问题快速定位。

什么是harness？
            用户请求
               ↓
        ┌──────────────┐
        │   Harness    │
        │              │
        │ 上下文管理    │
        │ 权限检查      │
        │ Prompt组装    │
        │ 工具调度      │
        │ 错误处理      │
        │ 重试/超时     │
        │ 日志追踪      │
        │ 状态管理      │
        └──────┬───────┘
               ↓
             LLM
               ↓
          要不要调用工具？
          ↙          ↘
       调工具        直接回答
          ↓
        Harness
          ↓
       再调用 LLM
          ↓
        最终回答

# MCP 接入

# 数据集构建：设计并落地基于大模型的半自动标注数据流水线，新场景数据构建效率提升约40%

配置：1个DINO、1个SAM、4个Qwen Worker、轮询间隔0.1秒、SAM热缓存。

| 并发 | 请求数 | 成功率 | 吞吐量 | P50 | **P95** | P99 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 1 | 20 | 100% | 0.210 req/s | 4.69s | 5.54s | 6.08s |
| 2 | 50 | 100% | 0.434 req/s | 4.43s | 5.34s | 5.85s |
| 4 | 50 | 100% | 0.786 req/s | 5.03s | 5.90s | 6.51s |
| 8 | 100 | 100% | 1.023 req/s | 7.57s | **8.91s** | 9.09s |
| 16 | 100 | 100% | 1.046 req/s | 14.82s | 16.42s | 16.71s |
