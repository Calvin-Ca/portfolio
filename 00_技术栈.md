# 数据集构建|大模型微调|模型部署

## 数据合成技术
- 是什么：数据集合成是一种通过规则、数据增强或生成模型自动构造训练样本的方法，用于**解决真实数据不足、标注成本高以及长尾场景覆盖不足**的问题。常见方法包括**基于规则模板的数据生成**、图像/文本增强，以及基于GAN、Diffusion和**大模型**的数据生成。在实际应用中通常不会直接使用全部合成数据，而是结合真实数据，通过**去重、质量评估和人工审核**保证数据分布一致性。

- 如何保证有效：如何保证合成数据质量？
  格式校验：json有无乱码，token长度超限，字段是否缺失
  去重，LLM容易生成重复数据：simhash 适合表层文字相似，不擅长语义改写，embedding反之
  分布一致性检查：类别分布、特征分布。文本用embedding，图片可用clip。计算KL散度
  人工抽检

# Agent 应用开发
## langgraph
关键变量：state、node、edge

| 类别 | 常见变量/对象 | 用途 |
|---|---|---|
| 共享状态 | `state` | 图中各节点读写的数据，如 `messages`、`user_input`、`result`、`next` |
| 状态结构 | `State` / `MessagesState` | 定义 `state` 允许有哪些字段及类型 |
| 消息历史 | `messages` | Agent 对话的短期记忆；通常配合消息 reducer 追加，而非覆盖 |
| 节点 | `node` / `agent` / `tool_node` | 执行 LLM 调用、工具调用、数据处理等逻辑的函数 |
| 路由 | `route` / `should_continue` / `next` | 根据当前状态决定下一个节点或结束 |
| 图构建器 | `builder` / `graph` | `StateGraph(State)`，用于注册节点和边 |
| 起止标志 | `START`、`END` | 图的入口和终止位置 |
| 运行配置 | `config` | 调用级配置，最重要的是 `configurable.thread_id` |
| 运行时上下文 | `runtime.context` | 不应写入持久状态的依赖/配置，如当前用户、模型实例、数据库连接 |
| 持久化 | `checkpointer` | 保存每一步 state，用于多轮会话、恢复执行、人机协作 |
| 长期记忆 | `store` | 跨线程、跨会话的数据存储；区别于当前图的 `state` |
| 控制流 | `Command` / `Send` | `Command` 同时更新 state 和跳转；`Send` 用于动态并行/map-reduce |
| 人工中断 | `interrupt()` | 暂停图执行，等待人工输入后恢复 |

| 实现 | `checkpointer` 实际位置 | `store` 实际位置 | 适用场景 |
|---|---|---|---|
| `InMemorySaver` / `InMemoryStore` | 当前 Python 进程内存 | 当前 Python 进程内存 | 本地调试；重启即丢失 |
| SQLite 实现 | 本地 `.sqlite` 数据库文件 | 本地 `.sqlite` 数据库文件 | 单机开发、小型服务 |
| Postgres 实现 | PostgreSQL 的 checkpoint 相关表 | PostgreSQL 的 store 相关表 | 生产环境，多实例共享 |
| LangGraph Platform / Server | 平台配置的后端数据库 | 平台配置的后端数据库 | 托管或自部署运行环境 |

## 协议/约定

| 场景 | 协议 / 约定 | 核心内容 |
|---|---|---|
| Agent 调外部工具、数据源 | MCP | Server 暴露 `tools`、`resources`、`prompts`；Agent 以统一方式发现并调用能力。 |
| 多 Agent 协作 | A2A | Agent 通过 `Agent Card` 声明能力、认证和地址；以 Task/Message 交互，支持流式、异步轮询、取消及结果产物。 |
| 模型调用工具 | Function Calling / Tool Calling | 工具用 JSON Schema 定义输入；模型返回工具名与参数，程序校验后执行并把结果回传模型。 |
| 稳定机器可读输出 | Structured Outputs | 用 JSON Schema 限定输出，例如工单分类、字段抽取、路由结果。 |
| 认证与授权 | OAuth 2.0 / OIDC、API Key、RBAC | 明确主体可调用的工具/资源范围；Token 应最小权限、短期有效、可审计。 |
| 长任务与恢复 | `task_id` / `thread_id` / checkpoint | 任务应有唯一 ID、状态机（pending/running/completed/failed/cancelled）、幂等键及可恢复快照。 |
| 人机协作 | approval / interrupt | 发邮件、删除、下单、付款、发布等副作用操作，应展示影响范围后等待确认。 |
| 工具执行安全 | 校验、超时、重试、幂等、补偿 | 参数先 Schema 校验；写操作带幂等键；失败可重试或人工介入。 |
| 上下文与记忆 | `state`、`context`、`store` 边界 | `state` 管单次流程，`context` 放运行时依赖，`store` 放跨会话长期记忆。 |
| 可观测性 | `trace_id` / `run_id` / `correlation_id` | 串联模型调用、工具调用和状态迁移，记录耗时、token、错误与权限决策。 |


## openAI 兼容API

| 类型 | 典型 Endpoint | 用途 |
|---|---|---|
| 对话生成 | `/v1/chat/completions` | 传统消息格式：`messages`、`model`、`temperature`；兼容面最广 |
| Agent / 多模态 | `/v1/responses` | 新一代统一接口，支持文本、图片、工具调用、上下文延续与流式输出 |
| 工具调用 | `tools` | 用 JSON Schema 声明函数参数，模型返回 `tool_call`，业务端执行后回传结果 |
| 结构化输出 | `response_format` / JSON Schema | 强制模型按指定 JSON 结构输出，适合抽取、分类、路由 |
| 向量化 | `/v1/embeddings` | 将文本转为向量，用于 RAG 检索、相似度搜索、聚类 |
| 图像 | `/v1/images/generations`、`/v1/images/edits` | 文生图、图像编辑 |
| 音频 | `/v1/audio/speech`、`/v1/audio/transcriptions` | TTS 和语音转文字 |
| 实时语音 | `/v1/realtime` | WebSocket / WebRTC / SIP 的低延迟多模态交互 |
| 内容安全 | `/v1/moderations` | 对文本或图片做风险分类 |
| 异步批处理 | `/v1/batches` | 大规模离线请求，异步提交和获取结果 |
| 模型微调 | `/v1/fine_tuning/jobs` | 创建、查询和管理微调任务 |

## 
